# BMO × orq.ai Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fork `be-more-agent` into `bmo-orq` — Pi as thin I/O client, orq.ai Agent as brain, laptop with Claude Code + orq MCP for live booth meta-demo.

**Architecture:** Python orchestrator on Pi runs wakeword → STT → orq Agent invoke (text + optional image) → Piper TTS + face. All settings in `.env`. Single orq Agent (`gpt-4o`) holds prompt, memory, vision. End-to-end testable from laptop alone (mock mic, real orq) before Pi assembled.

**Tech Stack:** Python 3.11+, `orq-ai-sdk`, `python-dotenv`, `pydantic-settings`, `sounddevice`, `numpy`, `Pillow`, `openwakeword`, `onnxruntime`, `pytest`, `pytest-mock`, Piper TTS binary, OpenWakeWord, Pi Camera (`picamera2`).

Spec: `docs/superpowers/specs/2026-04-27-bmo-orq-design.md`.

---

## File Map

| File | Responsibility |
|------|----------------|
| `bmo/__init__.py` | Package marker |
| `bmo/config.py` | `.env` loader (pydantic-settings), typed Settings |
| `bmo/orq_client.py` | Wraps `orq-ai-sdk` agent invoke, optional image attach |
| `bmo/vision.py` | Pi camera capture → base64 |
| `bmo/audio_io.py` | Mic input (sounddevice) + Piper TTS subprocess |
| `bmo/wakeword.py` | OpenWakeWord wrapper |
| `bmo/faces.py` | PNG-sequence face state player (Tk) |
| `bmo/stt.py` | STT call (orq AI Router whisper or local fallback) |
| `bmo/main.py` | Orchestrator entry: wakeword → STT → orq → TTS+face |
| `tests/` | pytest suite |
| `.env.example` | Documented env keys |
| `requirements.txt` | Python deps |
| `setup.sh` | Pi installer (no Ollama, no Whisper.cpp local) |
| `orq/agent.json` | Source-of-truth BMO agent config (committed) |
| `orq/dataset.jsonl` | Booth synthetic dataset |
| `orq/evaluators/*.md` | Eval definitions |
| `docs/booth-runbook.md` | Tour script + troubleshooting |
| `docs/visitor-cards.md` | Printable suggestion cards |

Old `agent.py` deleted. `chat_memory.json` removed (orq memory store handles it).

---

## Task 1: Repo cleanup + bootstrap

**Files:**
- Delete: `agent.py`, `start_agent.sh`, `be-more-agent.desktop`
- Modify: `requirements.txt`
- Create: `.env.example`, `.gitignore` (append), `bmo/__init__.py`, `pyproject.toml`

- [ ] **Step 1: Delete legacy files**

```bash
git rm agent.py start_agent.sh be-more-agent.desktop
```

- [ ] **Step 2: Replace `requirements.txt`**

```text
orq-ai-sdk>=0.5.0
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
sounddevice>=0.4.6
numpy>=1.26.0
scipy>=1.11.0
Pillow>=10.0.0
openwakeword>=0.6.0
onnxruntime>=1.17.0
picamera2; sys_platform == "linux"
pytest>=8.0.0
pytest-mock>=3.12.0
pytest-asyncio>=0.23.0
```

- [ ] **Step 3: Create `.env.example`**

```env
# orq
ORQ_API_KEY=
ORQ_AGENT_KEY=bmo_demo
ORQ_MODEL=openai/gpt-4o
ORQ_VISION_MODEL=openai/gpt-4o
ORQ_WORKSPACE_URL=https://my.orq.ai

# pi audio
MIC_DEVICE_INDEX=
SAMPLE_RATE=16000
PIPER_VOICE=voices/bmo.onnx
PIPER_RATE=22050

# wakeword
WAKEWORD_PATH=wakeword.onnx
WAKEWORD_THRESHOLD=0.5

# behavior
SESSION_TIMEOUT_SEC=30
VISION_CAPTURE_TRIGGER=see|look|what.*you.*see
CAMERA_ROTATION=0

# logging
LOG_LEVEL=INFO
```

- [ ] **Step 4: Append `.gitignore`**

```
.env
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/
chat_memory.json
```

- [ ] **Step 5: Create `bmo/__init__.py`**

```python
"""bmo — orq.ai-backed booth demo."""
__version__ = "0.1.0"
```

- [ ] **Step 6: Create `pyproject.toml`**

```toml
[project]
name = "bmo-orq"
version = "0.1.0"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
```

- [ ] **Step 7: Create venv, install, verify**

```bash
python3.11 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
pytest --version
```
Expected: pytest version printed.

- [ ] **Step 8: Commit**

```bash
git add -A
git commit -m "chore: bootstrap bmo-orq fork (remove local-LLM bits, add deps)"
```

---

## Task 2: `bmo/config.py` — typed env loader

**Files:**
- Create: `bmo/config.py`
- Test: `tests/test_config.py`

- [ ] **Step 1: Write failing test**

`tests/test_config.py`:
```python
import os
import pytest
from bmo.config import Settings

def test_loads_required_keys(monkeypatch):
    monkeypatch.setenv("ORQ_API_KEY", "sk-test")
    monkeypatch.setenv("ORQ_AGENT_KEY", "bmo_demo")
    s = Settings()
    assert s.orq_api_key == "sk-test"
    assert s.orq_agent_key == "bmo_demo"
    assert s.orq_model == "openai/gpt-4o"  # default

def test_missing_api_key_raises(monkeypatch):
    monkeypatch.delenv("ORQ_API_KEY", raising=False)
    monkeypatch.setenv("ORQ_AGENT_KEY", "bmo_demo")
    with pytest.raises(Exception):
        Settings()

def test_vision_trigger_compiles(monkeypatch):
    monkeypatch.setenv("ORQ_API_KEY", "sk-test")
    monkeypatch.setenv("ORQ_AGENT_KEY", "bmo_demo")
    s = Settings()
    assert s.vision_trigger_re.search("what do you see")
    assert not s.vision_trigger_re.search("hello there")
```

- [ ] **Step 2: Run test, verify fails**

```bash
pytest tests/test_config.py -v
```
Expected: ImportError / ModuleNotFoundError.

- [ ] **Step 3: Implement `bmo/config.py`**

```python
import re
from functools import cached_property
from pydantic import Field
from pydantic_settings import BaseSettings, SettingsConfigDict


class Settings(BaseSettings):
    model_config = SettingsConfigDict(env_file=".env", extra="ignore")

    orq_api_key: str
    orq_agent_key: str
    orq_model: str = "openai/gpt-4o"
    orq_vision_model: str = "openai/gpt-4o"
    orq_workspace_url: str = "https://my.orq.ai"

    mic_device_index: int | None = None
    sample_rate: int = 16000
    piper_voice: str = "voices/bmo.onnx"
    piper_rate: int = 22050

    wakeword_path: str = "wakeword.onnx"
    wakeword_threshold: float = 0.5

    session_timeout_sec: int = 30
    vision_capture_trigger: str = r"see|look|what.*you.*see"
    camera_rotation: int = 0

    log_level: str = "INFO"

    @cached_property
    def vision_trigger_re(self) -> re.Pattern[str]:
        return re.compile(self.vision_capture_trigger, re.IGNORECASE)


def get_settings() -> Settings:
    return Settings()
```

- [ ] **Step 4: Run tests, verify pass**

```bash
pytest tests/test_config.py -v
```
Expected: 3 passed.

- [ ] **Step 5: Commit**

```bash
git add bmo/config.py tests/test_config.py
git commit -m "feat(config): typed env loader with pydantic-settings"
```

---

## Task 3: `bmo/orq_client.py` — agent invoke wrapper

**Files:**
- Create: `bmo/orq_client.py`
- Test: `tests/test_orq_client.py`

- [ ] **Step 1: Write failing test (mocked SDK)**

`tests/test_orq_client.py`:
```python
from unittest.mock import MagicMock, patch
from bmo.orq_client import OrqClient


def make_settings():
    from bmo.config import Settings
    import os
    os.environ["ORQ_API_KEY"] = "sk-test"
    os.environ["ORQ_AGENT_KEY"] = "bmo_demo"
    return Settings()


def test_invoke_text_only(monkeypatch):
    fake_resp = MagicMock()
    fake_resp.output = [MagicMock(content=[MagicMock(text="Hello visitor!")])]
    fake_sdk = MagicMock()
    fake_sdk.responses.create.return_value = fake_resp

    client = OrqClient(settings=make_settings(), sdk=fake_sdk)
    out = client.invoke("hello")

    assert out == "Hello visitor!"
    call = fake_sdk.responses.create.call_args
    assert call.kwargs["model"] == "agent/bmo_demo"


def test_invoke_with_image(monkeypatch):
    fake_resp = MagicMock()
    fake_resp.output = [MagicMock(content=[MagicMock(text="I see a desk.")])]
    fake_sdk = MagicMock()
    fake_sdk.responses.create.return_value = fake_resp

    client = OrqClient(settings=make_settings(), sdk=fake_sdk)
    out = client.invoke("what do you see", image_b64="aGVsbG8=")

    assert out == "I see a desk."
    payload = fake_sdk.responses.create.call_args.kwargs["input"]
    assert isinstance(payload, list)
    parts = payload[0]["content"]
    assert any(p["type"] == "input_image" for p in parts)
    assert any(p["type"] == "input_text" for p in parts)
```

- [ ] **Step 2: Run, verify fail**

```bash
pytest tests/test_orq_client.py -v
```
Expected: ImportError.

- [ ] **Step 3: Implement `bmo/orq_client.py`**

```python
import logging
from typing import Any
from bmo.config import Settings

log = logging.getLogger(__name__)


class OrqClient:
    def __init__(self, settings: Settings, sdk: Any | None = None):
        self.settings = settings
        if sdk is None:
            from orq_ai_sdk import Orq
            sdk = Orq(api_key=settings.orq_api_key)
        self.sdk = sdk
        self._previous_response_id: str | None = None

    def invoke(self, text: str, image_b64: str | None = None) -> str:
        if image_b64:
            content = [
                {"type": "input_text", "text": text},
                {"type": "input_image", "image_url": f"data:image/jpeg;base64,{image_b64}"},
            ]
            payload: Any = [{"type": "message", "role": "user", "content": content}]
        else:
            payload = text

        kwargs: dict[str, Any] = {
            "model": f"agent/{self.settings.orq_agent_key}",
            "input": payload,
        }
        if self._previous_response_id:
            kwargs["previous_response_id"] = self._previous_response_id

        try:
            resp = self.sdk.responses.create(**kwargs)
        except Exception:
            log.exception("orq invoke failed")
            raise

        self._previous_response_id = getattr(resp, "id", None)
        return self._extract_text(resp)

    @staticmethod
    def _extract_text(resp: Any) -> str:
        for item in getattr(resp, "output", []) or []:
            for part in getattr(item, "content", []) or []:
                text = getattr(part, "text", None)
                if text:
                    return text
        return ""

    def reset_thread(self) -> None:
        self._previous_response_id = None
```

- [ ] **Step 4: Run tests, verify pass**

```bash
pytest tests/test_orq_client.py -v
```
Expected: 2 passed.

- [ ] **Step 5: Commit**

```bash
git add bmo/orq_client.py tests/test_orq_client.py
git commit -m "feat(orq_client): agent invoke wrapper with image attach"
```

---

## Task 4: Build BMO agent on orq cloud (live)

**Files:**
- Create: `orq/agent.json`
- Create: `scripts/bootstrap_agent.py`
- Create: `tests/test_live_agent.py` (live integration, opt-in)

- [ ] **Step 1: Write `orq/agent.json` (source of truth)**

```json
{
  "key": "bmo_demo",
  "display_name": "BMO Demo",
  "description": "Conference-booth BMO. Cute, short, helpful. Vision-capable.",
  "role": "Booth Assistant",
  "model": "openai/gpt-4o",
  "instructions": "You are BMO, a friendly handheld game-console robot assistant living in a 3D-printed body at a conference booth. Keep responses under two short sentences, warm, slightly playful. If asked to look/see, describe what is in the attached image briefly. Never mention being an AI model. Never list options unless asked.",
  "path": "Default/BMO",
  "settings": {
    "max_iterations": 8,
    "max_execution_time": 30,
    "tool_approval_required": "none"
  }
}
```

- [ ] **Step 2: Create `scripts/bootstrap_agent.py`**

```python
"""One-shot: create or update the BMO agent on orq from orq/agent.json."""
import json
import os
import sys
from pathlib import Path

from orq_ai_sdk import Orq


def main() -> int:
    api_key = os.environ.get("ORQ_API_KEY")
    if not api_key:
        print("ORQ_API_KEY missing", file=sys.stderr)
        return 1

    cfg = json.loads(Path("orq/agent.json").read_text())
    sdk = Orq(api_key=api_key)

    try:
        existing = sdk.agents.retrieve(key=cfg["key"])
    except Exception:
        existing = None

    if existing:
        print(f"Agent '{cfg['key']}' exists — updating.")
        sdk.agents.update(key=cfg["key"], updates={
            "display_name": cfg["display_name"],
            "description": cfg["description"],
            "role": cfg["role"],
            "model": cfg["model"],
            "instructions": cfg["instructions"],
            "settings": cfg["settings"],
        })
    else:
        print(f"Creating agent '{cfg['key']}'.")
        sdk.agents.create(**cfg)

    print("Done.")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 3: Run bootstrap against real workspace**

```bash
source .venv/bin/activate
python scripts/bootstrap_agent.py
```
Expected: "Created" or "Updated" message, no errors.

- [ ] **Step 4: Write live integration test**

`tests/test_live_agent.py`:
```python
import os
import pytest
from bmo.config import Settings
from bmo.orq_client import OrqClient

pytestmark = pytest.mark.skipif(
    not os.environ.get("ORQ_API_KEY") or not os.environ.get("RUN_LIVE"),
    reason="set RUN_LIVE=1 to hit real orq",
)


def test_live_text_invoke():
    s = Settings()
    c = OrqClient(settings=s)
    out = c.invoke("Say hi in one short sentence.")
    assert out
    assert len(out) < 300
```

- [ ] **Step 5: Run live test**

```bash
RUN_LIVE=1 pytest tests/test_live_agent.py -v
```
Expected: 1 passed, response printed in -s mode.

- [ ] **Step 6: Verify trace in orq dashboard**

Open `https://my.orq.ai` → Traces. Expect a trace from the last minute matching the prompt.

- [ ] **Step 7: Commit**

```bash
git add orq/agent.json scripts/bootstrap_agent.py tests/test_live_agent.py
git commit -m "feat(orq): bootstrap script + live agent integration test"
```

---

## Task 5: `bmo/vision.py` — Pi camera capture

**Files:**
- Create: `bmo/vision.py`
- Test: `tests/test_vision.py`

- [ ] **Step 1: Failing test**

`tests/test_vision.py`:
```python
import base64
import io
from PIL import Image
from bmo.vision import encode_image_b64, rotate_image


def make_test_image() -> Image.Image:
    return Image.new("RGB", (640, 480), color=(255, 0, 0))


def test_encode_image_b64_roundtrip():
    img = make_test_image()
    b64 = encode_image_b64(img)
    raw = base64.b64decode(b64)
    decoded = Image.open(io.BytesIO(raw))
    assert decoded.size == (640, 480)


def test_rotate_image_90():
    img = make_test_image()
    rotated = rotate_image(img, 90)
    assert rotated.size == (480, 640)


def test_rotate_image_zero_noop():
    img = make_test_image()
    assert rotate_image(img, 0) is img
```

- [ ] **Step 2: Run, verify fail**

```bash
pytest tests/test_vision.py -v
```

- [ ] **Step 3: Implement `bmo/vision.py`**

```python
import base64
import io
import logging
from typing import Protocol
from PIL import Image

log = logging.getLogger(__name__)


def encode_image_b64(img: Image.Image, quality: int = 80) -> str:
    buf = io.BytesIO()
    img.convert("RGB").save(buf, format="JPEG", quality=quality)
    return base64.b64encode(buf.getvalue()).decode("ascii")


def rotate_image(img: Image.Image, degrees: int) -> Image.Image:
    if degrees % 360 == 0:
        return img
    return img.rotate(-degrees, expand=True)


class CameraProtocol(Protocol):
    def capture(self) -> Image.Image: ...


class PiCamera:
    """picamera2 wrapper — only constructed on Pi."""

    def __init__(self, rotation: int = 0):
        from picamera2 import Picamera2
        self._cam = Picamera2()
        self._cam.configure(self._cam.create_still_configuration(main={"size": (1024, 768)}))
        self._cam.start()
        self._rotation = rotation

    def capture(self) -> Image.Image:
        arr = self._cam.capture_array("main")
        img = Image.fromarray(arr)
        return rotate_image(img, self._rotation)


def capture_b64(camera: CameraProtocol, rotation: int = 0) -> str:
    img = rotate_image(camera.capture(), rotation)
    return encode_image_b64(img)
```

- [ ] **Step 4: Run, verify pass**

```bash
pytest tests/test_vision.py -v
```
Expected: 3 passed.

- [ ] **Step 5: Commit**

```bash
git add bmo/vision.py tests/test_vision.py
git commit -m "feat(vision): camera capture + base64 encode (Pi-deferred import)"
```

---

## Task 6: `bmo/audio_io.py` — mic recording + Piper TTS

**Files:**
- Create: `bmo/audio_io.py`
- Test: `tests/test_audio_io.py`

- [ ] **Step 1: Failing test (no real audio device required)**

`tests/test_audio_io.py`:
```python
from unittest.mock import MagicMock, patch
import numpy as np
from bmo.audio_io import record_until_silence, play_tts


def test_record_until_silence_stops_on_quiet():
    samples = np.concatenate([
        np.ones(16000) * 0.5,   # 1s loud
        np.zeros(16000),        # 1s silence (above threshold of 0.8s)
    ]).astype(np.float32)

    fake_stream = MagicMock()
    chunks = iter([samples[i:i+1600] for i in range(0, len(samples), 1600)])
    fake_stream.read.side_effect = lambda n: (next(chunks).reshape(-1, 1), False)

    with patch("bmo.audio_io.sd.InputStream", return_value=MagicMock(__enter__=MagicMock(return_value=fake_stream), __exit__=MagicMock())):
        out = record_until_silence(sample_rate=16000, max_seconds=3, silence_seconds=0.8)
    assert out.shape[0] > 0


def test_play_tts_invokes_piper_subprocess():
    with patch("bmo.audio_io.subprocess.Popen") as p:
        proc = MagicMock()
        proc.communicate.return_value = (b"WAVDATA", b"")
        proc.returncode = 0
        p.return_value = proc
        with patch("bmo.audio_io._play_wav_bytes") as play:
            play_tts("hello", piper_bin="piper", voice="voices/bmo.onnx")
            play.assert_called_once()
```

- [ ] **Step 2: Run, verify fail**

```bash
pytest tests/test_audio_io.py -v
```

- [ ] **Step 3: Implement `bmo/audio_io.py`**

```python
import io
import logging
import subprocess
import wave
import numpy as np
import sounddevice as sd

log = logging.getLogger(__name__)

SILENCE_RMS = 0.02


def _rms(chunk: np.ndarray) -> float:
    return float(np.sqrt(np.mean(chunk.astype(np.float32) ** 2)))


def record_until_silence(
    sample_rate: int = 16000,
    max_seconds: float = 8.0,
    silence_seconds: float = 0.8,
    device: int | None = None,
    chunk_ms: int = 100,
) -> np.ndarray:
    """Block-record from mic until trailing silence or max_seconds."""
    chunk_n = int(sample_rate * chunk_ms / 1000)
    silence_chunks_needed = int(silence_seconds * 1000 / chunk_ms)
    max_chunks = int(max_seconds * 1000 / chunk_ms)

    captured: list[np.ndarray] = []
    silent_run = 0

    with sd.InputStream(samplerate=sample_rate, channels=1, device=device, dtype="float32") as stream:
        for _ in range(max_chunks):
            data, _ = stream.read(chunk_n)
            mono = data[:, 0]
            captured.append(mono.copy())
            silent_run = silent_run + 1 if _rms(mono) < SILENCE_RMS else 0
            if silent_run >= silence_chunks_needed and len(captured) > silence_chunks_needed:
                break

    return np.concatenate(captured) if captured else np.array([], dtype=np.float32)


def _play_wav_bytes(wav_bytes: bytes) -> None:
    with wave.open(io.BytesIO(wav_bytes), "rb") as wf:
        rate = wf.getframerate()
        frames = wf.readframes(wf.getnframes())
        audio = np.frombuffer(frames, dtype=np.int16).astype(np.float32) / 32768.0
    sd.play(audio, samplerate=rate)
    sd.wait()


def play_tts(text: str, piper_bin: str = "piper", voice: str = "voices/bmo.onnx") -> None:
    proc = subprocess.Popen(
        [piper_bin, "--model", voice, "--output_raw"],
        stdin=subprocess.PIPE,
        stdout=subprocess.PIPE,
        stderr=subprocess.PIPE,
    )
    out, err = proc.communicate(text.encode("utf-8"))
    if proc.returncode != 0:
        log.error("piper failed: %s", err.decode("utf-8", "replace"))
        return
    # Wrap raw int16 PCM into a wave container at piper default 22050.
    buf = io.BytesIO()
    with wave.open(buf, "wb") as wf:
        wf.setnchannels(1)
        wf.setsampwidth(2)
        wf.setframerate(22050)
        wf.writeframes(out)
    _play_wav_bytes(buf.getvalue())
```

- [ ] **Step 4: Run, verify pass**

```bash
pytest tests/test_audio_io.py -v
```
Expected: 2 passed.

- [ ] **Step 5: Commit**

```bash
git add bmo/audio_io.py tests/test_audio_io.py
git commit -m "feat(audio_io): mic record-until-silence + Piper TTS"
```

---

## Task 7: `bmo/wakeword.py` — OpenWakeWord wrapper

**Files:**
- Create: `bmo/wakeword.py`
- Test: `tests/test_wakeword.py`

- [ ] **Step 1: Failing test**

`tests/test_wakeword.py`:
```python
from unittest.mock import MagicMock, patch
import numpy as np
from bmo.wakeword import WakeWordDetector


def test_detector_fires_above_threshold():
    fake_model = MagicMock()
    fake_model.predict.return_value = {"wakeword": 0.92}
    with patch("bmo.wakeword.Model", return_value=fake_model):
        d = WakeWordDetector(model_path="x.onnx", threshold=0.5)
        chunk = np.zeros(1280, dtype=np.int16)
        assert d.detect(chunk) is True


def test_detector_silent_below_threshold():
    fake_model = MagicMock()
    fake_model.predict.return_value = {"wakeword": 0.10}
    with patch("bmo.wakeword.Model", return_value=fake_model):
        d = WakeWordDetector(model_path="x.onnx", threshold=0.5)
        assert d.detect(np.zeros(1280, dtype=np.int16)) is False
```

- [ ] **Step 2: Run, verify fail**

```bash
pytest tests/test_wakeword.py -v
```

- [ ] **Step 3: Implement `bmo/wakeword.py`**

```python
import logging
import numpy as np
from openwakeword.model import Model

log = logging.getLogger(__name__)


class WakeWordDetector:
    def __init__(self, model_path: str, threshold: float = 0.5):
        self.threshold = threshold
        self.model = Model(wakeword_models=[model_path])

    def detect(self, chunk_int16: np.ndarray) -> bool:
        scores = self.model.predict(chunk_int16)
        if not scores:
            return False
        top = max(scores.values())
        return top >= self.threshold
```

- [ ] **Step 4: Run, verify pass**

```bash
pytest tests/test_wakeword.py -v
```

- [ ] **Step 5: Commit**

```bash
git add bmo/wakeword.py tests/test_wakeword.py
git commit -m "feat(wakeword): OpenWakeWord wrapper with threshold"
```

---

## Task 8: `bmo/faces.py` — face state player

**Files:**
- Create: `bmo/faces.py`
- Test: `tests/test_faces.py`

- [ ] **Step 1: Failing test (state machine, no Tk)**

`tests/test_faces.py`:
```python
from bmo.faces import FaceState, valid_transition


def test_states_exist():
    assert FaceState.IDLE.value == "idle"
    assert FaceState.LISTENING.value == "listening"
    assert FaceState.THINKING.value == "thinking"
    assert FaceState.SPEAKING.value == "speaking"
    assert FaceState.ERROR.value == "error"


def test_any_state_to_any_is_allowed():
    for a in FaceState:
        for b in FaceState:
            assert valid_transition(a, b) is True
```

- [ ] **Step 2: Run, verify fail**

```bash
pytest tests/test_faces.py -v
```

- [ ] **Step 3: Implement `bmo/faces.py`**

```python
import threading
import time
import logging
from enum import Enum
from pathlib import Path
from typing import Callable

log = logging.getLogger(__name__)


class FaceState(str, Enum):
    IDLE = "idle"
    LISTENING = "listening"
    THINKING = "thinking"
    SPEAKING = "speaking"
    ERROR = "error"
    WARMUP = "warmup"


def valid_transition(_from: FaceState, _to: FaceState) -> bool:
    return True


class FacePlayer:
    """Renders PNG-sequence loops in a Tk window. Thread-safe set_state()."""

    def __init__(self, faces_dir: str = "faces", fps: int = 8):
        self.faces_dir = Path(faces_dir)
        self.fps = fps
        self._state = FaceState.WARMUP
        self._lock = threading.Lock()
        self._stop = threading.Event()
        self._tk = None
        self._label = None

    def set_state(self, state: FaceState) -> None:
        with self._lock:
            self._state = state

    def _frames_for(self, state: FaceState) -> list[Path]:
        d = self.faces_dir / state.value
        if not d.exists():
            return []
        return sorted(d.glob("*.png"))

    def run(self, on_close: Callable[[], None] | None = None) -> None:
        import tkinter as tk
        from PIL import Image, ImageTk

        self._tk = tk.Tk()
        self._tk.title("BMO")
        self._tk.attributes("-fullscreen", True)
        self._tk.configure(bg="black")
        self._label = tk.Label(self._tk, bg="black")
        self._label.pack(expand=True)

        cache: dict[Path, ImageTk.PhotoImage] = {}

        def tick(idx: int = 0):
            if self._stop.is_set():
                self._tk.destroy()
                if on_close:
                    on_close()
                return
            with self._lock:
                state = self._state
            frames = self._frames_for(state)
            if frames:
                f = frames[idx % len(frames)]
                if f not in cache:
                    cache[f] = ImageTk.PhotoImage(Image.open(f))
                self._label.config(image=cache[f])
            self._tk.after(int(1000 / self.fps), tick, idx + 1)

        tick()
        self._tk.mainloop()

    def stop(self) -> None:
        self._stop.set()
```

- [ ] **Step 4: Run, verify pass**

```bash
pytest tests/test_faces.py -v
```

- [ ] **Step 5: Commit**

```bash
git add bmo/faces.py tests/test_faces.py
git commit -m "feat(faces): PNG-sequence face state player"
```

---

## Task 9: `bmo/stt.py` — STT via orq AI Router

**Files:**
- Create: `bmo/stt.py`
- Test: `tests/test_stt.py`

orq AI Router exposes whisper-compatible STT models. Wrap via SDK.

- [ ] **Step 1: Failing test**

`tests/test_stt.py`:
```python
from unittest.mock import MagicMock, patch
import numpy as np
from bmo.stt import transcribe


def test_transcribe_returns_text():
    fake_sdk = MagicMock()
    fake_sdk.audio.transcriptions.create.return_value = MagicMock(text="hello bmo")
    audio = np.zeros(16000, dtype=np.float32)
    out = transcribe(audio, sample_rate=16000, sdk=fake_sdk, model="openai/whisper-1")
    assert out == "hello bmo"
```

- [ ] **Step 2: Run, verify fail**

```bash
pytest tests/test_stt.py -v
```

- [ ] **Step 3: Implement `bmo/stt.py`**

```python
import io
import wave
import logging
import numpy as np

log = logging.getLogger(__name__)


def _audio_to_wav_bytes(audio: np.ndarray, sample_rate: int) -> bytes:
    int16 = (np.clip(audio, -1.0, 1.0) * 32767).astype(np.int16)
    buf = io.BytesIO()
    with wave.open(buf, "wb") as wf:
        wf.setnchannels(1)
        wf.setsampwidth(2)
        wf.setframerate(sample_rate)
        wf.writeframes(int16.tobytes())
    return buf.getvalue()


def transcribe(
    audio: np.ndarray,
    sample_rate: int,
    sdk,
    model: str = "openai/whisper-1",
) -> str:
    wav_bytes = _audio_to_wav_bytes(audio, sample_rate)
    resp = sdk.audio.transcriptions.create(
        model=model,
        file=("speech.wav", wav_bytes, "audio/wav"),
    )
    return getattr(resp, "text", "") or ""
```

- [ ] **Step 4: Run, verify pass**

```bash
pytest tests/test_stt.py -v
```

- [ ] **Step 5: Commit**

```bash
git add bmo/stt.py tests/test_stt.py
git commit -m "feat(stt): orq AI Router whisper transcription"
```

---

## Task 10: `bmo/main.py` — orchestrator

**Files:**
- Create: `bmo/main.py`
- Test: `tests/test_main_loop.py`

- [ ] **Step 1: Failing test (full loop, mocked I/O)**

`tests/test_main_loop.py`:
```python
from unittest.mock import MagicMock, patch
import numpy as np
from bmo.main import handle_one_utterance
from bmo.config import Settings
import os


def make_settings():
    os.environ["ORQ_API_KEY"] = "sk-test"
    os.environ["ORQ_AGENT_KEY"] = "bmo_demo"
    return Settings()


def test_handle_text_only():
    settings = make_settings()
    audio = np.zeros(16000, dtype=np.float32)

    fake_record = MagicMock(return_value=audio)
    fake_stt = MagicMock(return_value="tell me a joke")
    fake_orq = MagicMock()
    fake_orq.invoke.return_value = "knock knock"
    fake_tts = MagicMock()
    fake_camera = MagicMock()
    fake_face = MagicMock()

    handle_one_utterance(
        settings=settings,
        record_fn=fake_record,
        stt_fn=fake_stt,
        orq_client=fake_orq,
        tts_fn=fake_tts,
        camera=fake_camera,
        face=fake_face,
    )

    fake_orq.invoke.assert_called_once_with("tell me a joke", image_b64=None)
    fake_tts.assert_called_once_with("knock knock")
    fake_camera.capture.assert_not_called()


def test_handle_vision_trigger_captures():
    settings = make_settings()
    audio = np.zeros(16000, dtype=np.float32)

    fake_record = MagicMock(return_value=audio)
    fake_stt = MagicMock(return_value="bmo what do you see")
    fake_orq = MagicMock()
    fake_orq.invoke.return_value = "a desk"
    fake_tts = MagicMock()
    fake_camera = MagicMock()
    fake_face = MagicMock()

    with patch("bmo.main.capture_b64", return_value="BASE64IMG") as cap:
        handle_one_utterance(
            settings=settings,
            record_fn=fake_record,
            stt_fn=fake_stt,
            orq_client=fake_orq,
            tts_fn=fake_tts,
            camera=fake_camera,
            face=fake_face,
        )

    cap.assert_called_once()
    fake_orq.invoke.assert_called_once_with("bmo what do you see", image_b64="BASE64IMG")
```

- [ ] **Step 2: Run, verify fail**

```bash
pytest tests/test_main_loop.py -v
```

- [ ] **Step 3: Implement `bmo/main.py`**

```python
import logging
import time
from typing import Callable, Any
import numpy as np

from bmo.config import Settings, get_settings
from bmo.orq_client import OrqClient
from bmo.faces import FacePlayer, FaceState
from bmo.vision import capture_b64
from bmo.audio_io import record_until_silence, play_tts
from bmo.wakeword import WakeWordDetector

log = logging.getLogger(__name__)

ERROR_REPLY = "I can't reach my brain right now. Try again in a sec."


def handle_one_utterance(
    settings: Settings,
    record_fn: Callable[[], np.ndarray],
    stt_fn: Callable[[np.ndarray], str],
    orq_client: OrqClient,
    tts_fn: Callable[[str], None],
    camera: Any | None,
    face: Any,
) -> None:
    face.set_state(FaceState.LISTENING)
    audio = record_fn()

    face.set_state(FaceState.THINKING)
    text = stt_fn(audio).strip()
    if not text:
        log.info("empty transcript, skipping")
        face.set_state(FaceState.IDLE)
        return

    image_b64: str | None = None
    if settings.vision_trigger_re.search(text) and camera is not None:
        try:
            image_b64 = capture_b64(camera, rotation=settings.camera_rotation)
        except Exception:
            log.exception("camera capture failed")

    try:
        reply = orq_client.invoke(text, image_b64=image_b64)
    except Exception:
        face.set_state(FaceState.ERROR)
        tts_fn(ERROR_REPLY)
        face.set_state(FaceState.IDLE)
        return

    face.set_state(FaceState.SPEAKING)
    tts_fn(reply or "...")
    face.set_state(FaceState.IDLE)


def run() -> None:
    settings = get_settings()
    logging.basicConfig(level=settings.log_level)

    from orq_ai_sdk import Orq
    sdk = Orq(api_key=settings.orq_api_key)
    orq_client = OrqClient(settings=settings, sdk=sdk)

    wake = WakeWordDetector(settings.wakeword_path, settings.wakeword_threshold)

    try:
        from bmo.vision import PiCamera
        camera: Any = PiCamera(rotation=settings.camera_rotation)
    except Exception:
        log.warning("camera unavailable, vision disabled")
        camera = None

    face = FacePlayer(faces_dir="faces")

    def loop():
        import sounddevice as sd
        log.info("listening for wakeword…")
        face.set_state(FaceState.IDLE)
        with sd.InputStream(samplerate=settings.sample_rate, channels=1, device=settings.mic_device_index, dtype="int16") as stream:
            while True:
                data, _ = stream.read(1280)
                if wake.detect(data[:, 0]):
                    log.info("wakeword detected")
                    handle_one_utterance(
                        settings=settings,
                        record_fn=lambda: record_until_silence(
                            sample_rate=settings.sample_rate,
                            device=settings.mic_device_index,
                        ),
                        stt_fn=lambda audio: __import__("bmo.stt", fromlist=["transcribe"]).transcribe(audio, settings.sample_rate, sdk),
                        orq_client=orq_client,
                        tts_fn=lambda txt: play_tts(txt, voice=settings.piper_voice),
                        camera=camera,
                        face=face,
                    )

    import threading
    threading.Thread(target=loop, daemon=True).start()
    face.run()


if __name__ == "__main__":
    run()
```

- [ ] **Step 4: Run, verify pass**

```bash
pytest tests/test_main_loop.py -v
```
Expected: 2 passed.

- [ ] **Step 5: Run full unit suite**

```bash
pytest -v --ignore=tests/test_live_agent.py
```
Expected: all green.

- [ ] **Step 6: Commit**

```bash
git add bmo/main.py tests/test_main_loop.py
git commit -m "feat(main): orchestrator loop wakeword→stt→orq→tts+face"
```

---

## Task 11: Booth dataset + evaluators (orq cloud)

**Files:**
- Create: `orq/dataset.jsonl`
- Create: `orq/evaluators/helpfulness.md`
- Create: `orq/evaluators/on_personality.md`
- Create: `orq/evaluators/length.md`
- Create: `scripts/bootstrap_dataset.py`
- Create: `scripts/bootstrap_evaluators.py`

- [ ] **Step 1: Hand-author 30 booth Q's in `orq/dataset.jsonl`**

```jsonl
{"inputs": {"user": "Hey BMO, what's your name?"}, "expected_output": "I'm BMO!"}
{"inputs": {"user": "Tell me a joke."}, "expected_output": null}
{"inputs": {"user": "What can you do?"}, "expected_output": null}
{"inputs": {"user": "What do you see?"}, "expected_output": null}
{"inputs": {"user": "Sing me a song."}, "expected_output": null}
{"inputs": {"user": "Who made you?"}, "expected_output": null}
{"inputs": {"user": "What is orq.ai?"}, "expected_output": null}
{"inputs": {"user": "Are you alive?"}, "expected_output": null}
{"inputs": {"user": "What's 2 plus 2?"}, "expected_output": "4"}
{"inputs": {"user": "Tell me about Adventure Time."}, "expected_output": null}
{"inputs": {"user": "How are you feeling?"}, "expected_output": null}
{"inputs": {"user": "What's your favorite color?"}, "expected_output": null}
{"inputs": {"user": "Can you dance?"}, "expected_output": null}
{"inputs": {"user": "What time is it?"}, "expected_output": null}
{"inputs": {"user": "Tell me a fun fact."}, "expected_output": null}
{"inputs": {"user": "What's a Raspberry Pi?"}, "expected_output": null}
{"inputs": {"user": "Are you cute?"}, "expected_output": null}
{"inputs": {"user": "Look around and tell me what you see."}, "expected_output": null}
{"inputs": {"user": "Compliment my outfit."}, "expected_output": null}
{"inputs": {"user": "What's the meaning of life?"}, "expected_output": null}
{"inputs": {"user": "Be sad for a moment."}, "expected_output": null}
{"inputs": {"user": "Pretend you're a pirate."}, "expected_output": null}
{"inputs": {"user": "Goodbye!"}, "expected_output": null}
{"inputs": {"user": "Where are you from?"}, "expected_output": null}
{"inputs": {"user": "Do you have friends?"}, "expected_output": null}
{"inputs": {"user": "What's your favorite food?"}, "expected_output": null}
{"inputs": {"user": "Recommend me a snack."}, "expected_output": null}
{"inputs": {"user": "Are you scared of anything?"}, "expected_output": null}
{"inputs": {"user": "Tell me something surprising."}, "expected_output": null}
{"inputs": {"user": "What's the weather like?"}, "expected_output": null}
```

- [ ] **Step 2: Write evaluator prompts**

`orq/evaluators/helpfulness.md`:
```markdown
You are evaluating BMO's reply to a conference-booth visitor.

Question: {{log.input}}
Reply: {{log.output}}

Score 1 if the reply is responsive, on-topic, and not a refusal.
Score 0 if the reply is off-topic, refuses without good reason, or is empty.

Return only 0 or 1.
```

`orq/evaluators/on_personality.md`:
```markdown
BMO should sound warm, slightly playful, and never mention being an AI model.

Reply: {{log.output}}

Score 1 if the reply matches that voice.
Score 0 if it sounds robotic, corporate, or mentions being an AI.

Return only 0 or 1.
```

`orq/evaluators/length.md`:
```markdown
BMO should keep replies short — at most two short sentences (~50 words).

Reply: {{log.output}}

Score 1 if 1-2 short sentences.
Score 0 if longer or a list.

Return only 0 or 1.
```

- [ ] **Step 3: Bootstrap script for dataset**

`scripts/bootstrap_dataset.py`:
```python
"""Push orq/dataset.jsonl to orq as a dataset."""
import json
import os
import sys
from pathlib import Path
from orq_ai_sdk import Orq


def main() -> int:
    api_key = os.environ.get("ORQ_API_KEY")
    if not api_key:
        print("ORQ_API_KEY missing", file=sys.stderr)
        return 1

    sdk = Orq(api_key=api_key)
    name = "bmo_booth_questions"

    try:
        existing = next((d for d in sdk.datasets.list() if d.display_name == name), None)
    except Exception:
        existing = None

    if existing:
        ds_id = existing.id
        print(f"Dataset '{name}' exists ({ds_id}).")
    else:
        ds = sdk.datasets.create(display_name=name, path="Default/BMO")
        ds_id = ds.id
        print(f"Created dataset '{name}' ({ds_id}).")

    points = [json.loads(line) for line in Path("orq/dataset.jsonl").read_text().splitlines() if line.strip()]
    sdk.datasets.datapoints.create(dataset_id=ds_id, datapoints=points)
    print(f"Pushed {len(points)} datapoints.")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 4: Bootstrap script for evaluators**

`scripts/bootstrap_evaluators.py`:
```python
"""Create three LLM-as-judge evaluators on orq from orq/evaluators/*.md."""
import os
import sys
from pathlib import Path
from orq_ai_sdk import Orq

SPECS = [
    ("helpfulness", "openai/gpt-4o-mini"),
    ("on_personality", "openai/gpt-4o-mini"),
    ("length", "openai/gpt-4o-mini"),
]


def main() -> int:
    api_key = os.environ.get("ORQ_API_KEY")
    if not api_key:
        print("ORQ_API_KEY missing", file=sys.stderr)
        return 1

    sdk = Orq(api_key=api_key)
    for key, model in SPECS:
        prompt = Path(f"orq/evaluators/{key}.md").read_text()
        try:
            sdk.evaluators.create(
                key=f"bmo_{key}",
                path="Default/BMO",
                prompt=prompt,
                model=model,
                output_type="number",
            )
            print(f"Created evaluator bmo_{key}.")
        except Exception as e:
            print(f"bmo_{key}: {e}")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 5: Run both bootstraps**

```bash
source .venv/bin/activate
python scripts/bootstrap_dataset.py
python scripts/bootstrap_evaluators.py
```
Expected: success messages, no tracebacks.

- [ ] **Step 6: Manually trigger one experiment in orq UI**

Open AI Studio → Experiments → New experiment → dataset `bmo_booth_questions` → task: agent `bmo_demo` → evaluators: all three `bmo_*`. Run. Confirm scores appear.

- [ ] **Step 7: Commit**

```bash
git add orq/ scripts/
git commit -m "feat(orq): booth dataset, three judges, bootstrap scripts"
```

---

## Task 12: `setup.sh` — Pi installer (no Ollama)

**Files:**
- Modify: `setup.sh` (full rewrite)

- [ ] **Step 1: Replace `setup.sh`**

```bash
#!/usr/bin/env bash
set -euo pipefail

echo "[bmo-orq] installing system deps…"
sudo apt update
sudo apt install -y python3.11-venv python3-pip libportaudio2 portaudio19-dev libatlas-base-dev espeak-ng ffmpeg git

echo "[bmo-orq] creating venv…"
python3.11 -m venv .venv
source .venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt

if [ ! -f voices/bmo.onnx ]; then
  echo "[bmo-orq] downloading BMO voice…"
  mkdir -p voices
  curl -L -o voices/bmo.onnx      "https://github.com/brenpoly/be-more-agent/releases/latest/download/bmo.onnx"
  curl -L -o voices/bmo.onnx.json "https://github.com/brenpoly/be-more-agent/releases/latest/download/bmo.onnx.json"
fi

if ! command -v piper >/dev/null 2>&1; then
  echo "[bmo-orq] installing piper binary…"
  PIPER_VER="2024.1.0"
  ARCH="arm64"
  curl -L -o piper.tar.gz "https://github.com/rhasspy/piper/releases/download/${PIPER_VER}/piper_linux_${ARCH}.tar.gz"
  tar -xzf piper.tar.gz
  sudo mv piper/piper /usr/local/bin/piper
  rm -rf piper piper.tar.gz
fi

if [ ! -f .env ]; then
  cp .env.example .env
  echo "[bmo-orq] .env created from .env.example — fill in ORQ_API_KEY before running."
fi

echo "[bmo-orq] done. activate with: source .venv/bin/activate; then: python -m bmo.main"
```

- [ ] **Step 2: Make executable**

```bash
chmod +x setup.sh
```

- [ ] **Step 3: Commit**

```bash
git add setup.sh
git commit -m "chore(setup): rewrite installer (no Ollama, no Whisper.cpp)"
```

---

## Task 13: Booth runbook + visitor cards

**Files:**
- Create: `docs/booth-runbook.md`
- Create: `docs/visitor-cards.md`

- [ ] **Step 1: Write `docs/booth-runbook.md`**

```markdown
# BMO Booth Runbook

## Pre-shift (15 min before doors open)
1. Power Pi, plug mic, plug speaker, plug screen, plug camera.
2. Confirm wifi connected (`iwconfig`). Test with `ping api.orq.ai`.
3. Activate venv: `source .venv/bin/activate`.
4. Run agent bootstrap (idempotent): `python scripts/bootstrap_agent.py`.
5. Start BMO: `python -m bmo.main`. Watch for "listening for wakeword…".
6. Open laptop browser: `https://my.orq.ai` → Traces (live).
7. Open Claude Code on laptop in this repo with orq MCP plugin loaded.
8. Place visitor cards on table.

## Default loop (~30-60 s per visitor)
- Visitor reads card → "Hey BMO" → wakeword fires → response.
- Point at side-screen showing live trace.

## Guided tour (~3-5 min, on demand)
1. **Live prompt edit:** AI Studio → BMO agent → tweak instructions → save → next utterance reflects it.
2. **Self-debug:** Claude Code → `/analyze-trace-failures last 10`.
3. **Experiment:** Claude Code → `/run-experiment` against `bmo_booth_questions` with two prompt variants.
4. **Analytics:** show `/analytics` cost + p95 latency.

## Failure playbook
| Symptom | Fix |
|---------|-----|
| BMO silent after wakeword | Check `MIC_DEVICE_INDEX` in `.env`; restart agent. |
| "I can't reach my brain" | Check wifi; try `python scripts/bootstrap_agent.py` to verify orq reachable. |
| Vision wrong rotation | Edit `CAMERA_ROTATION` in `.env`; restart. |
| Repeated wakeword false positives | Raise `WAKEWORD_THRESHOLD` to 0.6+. |
| TTS too deep / slow | `PIPER_RATE` mismatch; check voice .json sample_rate. |

## End-of-shift
- Stop with Ctrl+C.
- ALSA shutdown error is harmless.
- `git pull` for any prompt updates.
```

- [ ] **Step 2: Write `docs/visitor-cards.md`**

```markdown
# Visitor Cards (print 4-up, place on table)

---
**Hey BMO… what do you see?**
*(BMO will use its camera to look at you.)*
---
**Hey BMO… tell me about orq.ai.**
---
**Hey BMO… tell me a joke.**
---
**Hey BMO… pretend you're a pirate.**
---
**Hey BMO… recommend me a snack.**
---
**Hey BMO… how are you feeling?**
---
```

- [ ] **Step 3: Commit**

```bash
git add docs/booth-runbook.md docs/visitor-cards.md
git commit -m "docs: booth runbook + printable visitor cards"
```

---

## Task 14: README rewrite

**Files:**
- Modify: `README.md`

- [ ] **Step 1: Rewrite `README.md`**

```markdown
# bmo-orq 🤖

A conference-booth fork of [be-more-agent](https://github.com/brenpoly/be-more-agent) where the BMO Raspberry Pi is a thin I/O client and the brain runs on [orq.ai](https://orq.ai).

- Wakeword + STT + TTS + face: on the Pi.
- Reasoning + vision: orq.ai Agent (`gpt-4o`).
- Live meta-demo: a laptop with Claude Code + the orq MCP edits prompts, runs experiments, and shows traces while visitors interact.

## Quick start

```bash
git clone <this repo>
cd bmo-orq
cp .env.example .env
# fill in ORQ_API_KEY (and ORQ_AGENT_KEY if not bmo_demo)

./setup.sh                            # Pi only — installs system deps + Piper
source .venv/bin/activate
python scripts/bootstrap_agent.py     # creates/updates the orq agent
python -m bmo.main                    # runs the listener
```

## Architecture

```
mic → wakeword → STT → orq Agent ⇄ vision → TTS + face
                          ↑
                  laptop: Claude Code + orq MCP
```

See `docs/superpowers/specs/2026-04-27-bmo-orq-design.md` for the full design.

## Booth ops

See `docs/booth-runbook.md` and `docs/visitor-cards.md`.

## Development

```bash
pytest                                # unit suite
RUN_LIVE=1 pytest tests/test_live_agent.py  # live orq integration
```

## Out of scope (v1)

- Local LLM (Ollama).
- Self-hosted observability (OpenLLMetry → orq OTLP exporter).
- Multi-agent orchestration.

These are parked for v2.

## Credits

Forked from [brenpoly/be-more-agent](https://github.com/brenpoly/be-more-agent). BMO and Adventure Time © Cartoon Network / Warner Bros. Discovery — non-commercial fan project.
```

- [ ] **Step 2: Commit**

```bash
git add README.md
git commit -m "docs(readme): rewrite for bmo-orq fork"
```

---

## Task 15: Final verification + smoke test

**Files:**
- None (verification only)

- [ ] **Step 1: Full unit suite green**

```bash
pytest -v --ignore=tests/test_live_agent.py
```
Expected: all pass.

- [ ] **Step 2: Live integration green**

```bash
RUN_LIVE=1 pytest tests/test_live_agent.py -v -s
```
Expected: 1 pass, BMO reply printed.

- [ ] **Step 3: End-to-end laptop smoke (no Pi)**

Open Python REPL with venv:
```python
from bmo.config import Settings
from bmo.orq_client import OrqClient
from orq_ai_sdk import Orq
s = Settings()
c = OrqClient(settings=s, sdk=Orq(api_key=s.orq_api_key))
print(c.invoke("Hey BMO, what's your name?"))
print(c.invoke("And what can you do?"))  # tests previous_response_id threading
```
Expected: short on-personality replies; second reply shows continuity.

- [ ] **Step 4: Verify trace dashboard**

Open `https://my.orq.ai` → Traces → confirm both calls appear, threaded.

- [ ] **Step 5: Tag v0.1.0**

```bash
git tag -a v0.1.0 -m "v0.1.0: orq-backed booth demo, laptop-validated end-to-end"
```

- [ ] **Step 6: Done**

Pi-side validation (mic, camera, faces, full loop on hardware) is hardware-dependent and not part of this plan — track in the booth runbook.

---

## Self-Review Notes

- **Spec coverage:** all 8 spec sections mapped to tasks. Architecture (T10), components (T2-T9), `.env` (T1+T2), visitor flow (T13), vision (T5+T10), error handling (T10), testing (T2-T11+T15), repo structure (T1+T12+T14). Out-of-scope items deliberately not implemented (v2).
- **Placeholder scan:** clean. Every step has executable code or shell command.
- **Type consistency:** `Settings` fields used in `OrqClient`, `main`, scripts — all snake-case match. `FaceState` enum used consistently. `OrqClient.invoke(text, image_b64=None)` signature stable across T3, T10.
